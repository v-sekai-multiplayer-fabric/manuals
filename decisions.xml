<?xml version="1.0" encoding="UTF-8"?>
<rss  xmlns:atom="http://www.w3.org/2005/Atom" 
      xmlns:media="http://search.yahoo.com/mrss/" 
      xmlns:content="http://purl.org/rss/1.0/modules/content/" 
      xmlns:dc="http://purl.org/dc/elements/1.1/" 
      version="2.0">
<channel>
<title>V-Sekai Fire — Manuals</title>
<link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions.html</link>
<atom:link href="https://v-sekai-multiplayer-fabric.github.io/manuals/decisions.xml" rel="self" type="application/rss+xml"/>
<description></description>
<generator>quarto-1.9.38</generator>
<lastBuildDate>Fri, 12 Jun 2026 00:00:00 GMT</lastBuildDate>
<item>
  <title>Android template builds with Temurin JDK 21 and config.gradle-pinned SDK</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260612-android-template-temurin-jdk21.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Android template builds with Temurin JDK 21 and config.gradle-pinned SDK</em></p>
<p><strong>Status:</strong> accepted</p>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The Quest 3 gate needs an Android export template of the merged double-precision assembly. The box carries no Android SDK, and Fedora 44 packages only JDK 25 and 26; Gradle rejects Java 25 with “Unsupported class file major version 69”.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: pin the Android toolchain to what the fork’s <code>platform/android/java/app/config.gradle</code> declares — NDK <code>29.0.14206865</code>, build-tools <code>36.1.0</code>, platform <code>android-36</code> — installed through <code>sdkmanager</code> from the command-line tools, and run Gradle on a Temurin JDK 21 tarball fetched from the Adoptium API, because the distribution ships no Gradle-compatible JDK and the tarball pins the version per checkout.</p>
<p>The verified sequence: <code>scons platform=android target=template_release arch=arm64 precision=double</code> produces <code>libgodot_android.so</code> (about four minutes on the workstation), then <code>JAVA_HOME=&lt;temurin-21&gt; ./gradlew generateGodotTemplates</code> produces <code>bin/android_release.apk</code>.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>The Quest 3 export template exists days before the gate, retiring the toolchain risk.</li>
<li><code>config.gradle</code> is the single source of truth for SDK versions; reading it first avoids guessing.</li>
<li>The JDK is per-user (<code>~/jdks</code>), so system Java stays at the distribution default.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p><code>bin/android_release.apk</code> builds from the merged assembly and installs on a Quest 3.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260612-android-template-temurin-jdk21.html</guid>
  <pubDate>Fri, 12 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Fabric channels as reliability classes, control in payload</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260612-fabric-channels-as-reliability-classes.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Fabric channels as reliability classes, control in payload</em></p>
<p><strong>Status:</strong> accepted</p>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>Transforms want unreliable, latest-wins delivery at tick rate; sparse control events (a teleport vote, a loot grab) want reliable, exactly-once delivery. Mixing both on one ordered stream lets a reliable event head-of-line-block the transform flow, and a separate text channel for control means two codecs and two reliability models to reason about.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: the fabric’s logical channels are the reliability classes, and the 100-byte entity packet carries everything. <code>FabricMultiplayerPeer</code> runs one ENet host per channel, so channels are head-of-line-free: CH_INTEREST (1) carries unreliable transforms, CH_MIGRATION (0) carries reliable-ordered control and state. The lane is chosen with <code>set_transfer_channel</code> plus <code>set_transfer_mode</code>; a <code>cmd</code>/<code>action</code> byte in the payload picks the meaning within a lane.</p>
<p>Control rides the same packet as the transform that produced it, so an action arrives bound to its position and HLC — the server validates a hit against the exact transform and tick, atomically. The transport gives exactly-once per channel, so there is no sequence counter, repeat, or dedupe. Server-to-client state (phase, grant, reject) uses the same format under reserved global ids (a zone entity for phase, the player entity for grants).</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>A reliable control event never stalls the unreliable transform stream, because each is a separate ENet host.</li>
<li>One codec and one wire format both directions; text disappears entirely.</li>
<li>A display name is a byte field in the payload, and bytes are integers, so even names stay integral and deterministic.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>The multi-session http3 fix (<a href="https://github.com/v-sekai-multiplayer-fabric/godot/pull/56">godot#56</a>) routes per-session replies, and a <code>FabricMultiplayerPeer</code> probe routes four concurrent clients with the ENet factories injected.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260612-fabric-channels-as-reliability-classes.html</guid>
  <pubDate>Fri, 12 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Five-second transaction limit (bounded operations)</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260612-five-second-transaction-limit.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Five-second transaction limit (bounded operations)</em></p>
<p><strong>Status:</strong> accepted</p>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>A connection that silently desyncs wastes a player’s time: the loop bug had a client believe it was joined while the server had dropped it, and it waited forever. FoundationDB bounds every transaction to five seconds and aborts past that, so a stuck operation fails fast instead of hanging. The slice wants the same guarantee across the operations that can block — connection recovery, the runtime MCP round trips, and any commit.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: no operation waits longer than five seconds; past that it aborts or self-heals. The connection state machine re-joins a client that stops hearing the server, and recovery is bounded — a <a href="https://github.com/v-sekai-multiplayer-fabric/connection-fsm">Lean plus Plausible model</a> proves the protocol sound (the client’s belief never disagrees with the server once settled) and complete within a five-second budget (an awake client returns to a healthy joined state within five ticks, one tick per second). The buggy protocol without the re-join rule is shown not complete: Plausible finds a permanent ghost that stays unhealthy after a hundred seconds. The runtime MCP operator calls carry a five-second deadline, and a commit that exceeds the budget aborts rather than blocking the loop.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>A desynced client recovers inside five seconds rather than hanging, so a player never waits on a dead connection.</li>
<li>The bound is a proof, not a hope: the model is the source of truth, and the loop client’s connection logic matches it (the liveliness window is fifteen seconds, the re-join delay four, so recovery lands inside five).</li>
<li>Operations that cannot finish in five seconds surface as failures the caller handles, which keeps the loop responsive.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p><code>lake exe fsm_demo</code> reports soundness and five-second recovery clean over a thirty-thousand-history sweep, with Plausible finding no counter-example for the fixed protocol and a ghost witness for the buggy one.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260612-five-second-transaction-limit.html</guid>
  <pubDate>Fri, 12 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Headless OpenXR testing with Monado (null compositor + simulated HMD)</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260612-headless-openxr-testing-with-monado.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Headless OpenXR testing with Monado (null compositor + simulated HMD)</em></p>
<p><strong>Status:</strong> accepted</p>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The OpenXR path needs to run on Linux without a headset: on the workstation for iteration and in a podman quadlet for CI. The standalone Quest 3 build is the performance gate (<a href="../decisions/20260611-quest-3-frame-floor-as-mvp-gate.html">the Quest 3 gate</a>), but functional and integration coverage of the OpenXR path needs a runnable runtime on the box, with no display and no hardware.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: run Monado with the null compositor and a simulated HMD, headless, because it is a conformant OpenXR runtime that an app reaches over the loader with no display and no device, on the workstation and inside a quadlet.</p>
<p>The recipe on Fedora (Monado 25.1.0):</p>
<ul>
<li>Install <code>monado monado-devel monado-vulkan-layers</code>.</li>
<li>Select the runtime: symlink <code>~/.config/openxr/1/active_runtime.json</code> to <code>/usr/share/openxr/1/openxr_monado.json</code>, or set <code>XR_RUNTIME_JSON</code>.</li>
<li>Start the service headless: <code>tail -f /dev/null | XRT_COMPOSITOR_NULL=1 SIMULATED_ENABLE=1 monado-service</code>.</li>
<li>OpenXR clients connect over the IPC socket <code>$XDG_RUNTIME_DIR/monado_comp_ipc</code>.</li>
</ul>
<p>The non-obvious part is the stdin pipe. <code>monado-service</code> watches stdin through <code>epoll</code> to notice shutdown, and a non-epoll-able stdin (a closed descriptor or a regular file, which is what a background job, a <code>nohup</code>, or a container hands it) makes <code>epoll_ctl(stdin)</code> fail and the service aborts before it opens the socket. Feeding it an epoll-able pipe (<code>tail -f /dev/null | ...</code>, or a held-open FIFO) is what lets it start.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>The OpenXR path runs on the workstation and in a podman quadlet with no display and no headset.</li>
<li><code>XRT_COMPOSITOR_NULL</code> discards submitted frames, so this covers the runtime, the tracking, and frame submission, with no rendered output and no performance signal.</li>
<li><code>SIMULATED_ENABLE</code> supplies a head and controllers driven programmatically; the <code>qwerty</code> driver (<code>QWERTY_ENABLE</code>) adds keyboard and mouse control for an interactive desktop run.</li>
<li>This is functional and integration coverage; the standalone Quest 3 build stays the performance and comfort gate.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p><code>openxr_runtime_list</code>, and any OpenXR app, reaches <code>xrCreateInstance</code> against the running service, and the service log reports the null compositor and a simulated HMD.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
<li><p><a href="20260611-quest-3-frame-floor-as-mvp-gate.html">Quest 3 frame floor as the MVP performance gate</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260612-headless-openxr-testing-with-monado.html</guid>
  <pubDate>Fri, 12 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Integral entity-transform wire (int64 micrometers, no origin shift)</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260612-integral-entity-transform-wire.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Integral entity-transform wire (int64 micrometers, no origin shift)</em></p>
<p><strong>Status:</strong> accepted</p>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The fabric replicates entity transforms in a fixed 100-byte packet (<code>XRGridEntityPacket</code>). The determinism doctrine keeps the authoritative state in integers and r128 fixed point, yet the packet carried position as <code>f64</code> — three doubles whose 52-bit mantissa cannot even represent a Q64.64 value. Floating point on the wire reintroduces the cross-platform divergence the determinism decision rules out.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: every field on the wire is integral. Position becomes <strong>int64 absolute micrometers</strong> — the integer twin of the <code>precision=double</code> large-world coordinate, so there is no camera-relative origin shifting (the double-precision build and r128 exist precisely to hold absolute coordinates). Velocity stays i16, scaled to <code>PBVH_V_MAX_PHYSICAL_DEFAULT</code> so it shares the predictive BVH’s units. Rotation stays i16 swing-twist. A 42-byte userdata payload carries control and state. The packet keeps its 100 bytes.</p>
<p>Position int64 micrometers spans plus or minus 9.2e12 m at 1 um, and matches the <a href="https://github.com/v-sekai-multiplayer-fabric/lean-predictive-bvh">lean-predictive-bvh</a> int64-um AABB space. The authoritative position stays r128; the wire is its micrometer-scale integer projection. Clients render rather than re-simulate (<a href="../decisions/20260611-server-authoritative-simulation-deferred-rollback.html">server authority</a>), so micrometer quantization is below perception while the exact state remains server-side.</p>
<p>A Lean 4 plus Plausible model in <a href="https://github.com/v-sekai-multiplayer-fabric/entity_packet">entity_packet</a> is the source of truth: a roundtrip property and a size invariant the C++ must match, plus a golden-vector differential.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>The wire has no floating point; replication is deterministic across platforms.</li>
<li>The packet speaks the predictive BVH’s native int64-micrometer language, so position flows into the BVH with no conversion.</li>
<li>Tying velocity to <code>PBVH_V_MAX_PHYSICAL_DEFAULT</code> closes a latent drift (the codec had an ad-hoc times-1000 scale that the BVH-sync review surfaced).</li>
<li>Density, when it matters, comes from value delta-from-baseline rather than origin rebasing; entropy coding stays off the per-tick path because variable length breaks the fixed datagram layout.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>The entity_packet Plausible suite passes a 50000-vector roundtrip and size sweep, and the engine’s C++ decode matches the Lean golden bytes on 64 vectors. The change lands on <code>feat/module-xr-grid</code> and <code>feat/module-multiplayer-fabric</code>.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
<li><p><a href="20260611-server-authoritative-simulation-deferred-rollback.html">Server-authoritative simulation with deferred rollback</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260612-integral-entity-transform-wire.html</guid>
  <pubDate>Fri, 12 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>MCP runtime bridge for deployed builds</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260612-mcp-runtime-bridge-deployed-builds.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › MCP runtime bridge for deployed builds</em></p>
<p><strong>Status:</strong> accepted</p>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The Godot MCP addon is an editor plugin, so it drives the editor over its <code>EditorInterface</code>. Debugging a deployed build — a headless zone server, a mobile client, a Quest 3 app — needs the same inspection against the running game, where there is no editor.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: a runtime autoload (<code>mcp_runtime.gd</code>) serves the same MCP from inside the running game, injecting the live <code>SceneTree</code> where the editor plugin injects the editor. An MCP client reaches it over <code>adb forward</code> (or any port forward). Scene, node, property, <code>call_method</code>, <code>run_script</code>, <code>get_render_info</code>, and a runtime-capable <code>screenshot</code> operate on the running game; editor-only commands return an error.</p>
<p>The contribution lands upstream as <a href="https://github.com/v-sekai-multiplayer-fabric/vsekai-godot-mcp/pull/1">vsekai-godot-mcp#1</a>.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>A deployed Quest build is inspectable and drivable from the workstation: frame stats, the live scene tree, and arbitrary GDScript against the running game.</li>
<li>Under XR, <code>screenshot</code> reads the flat Window viewport, which is black because the rendered frames go to the XR compositor’s per-eye swapchain and that is not host-readable; <code>get_render_info</code> and <code>run_script</code> cover XR diagnostics instead.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>On a Quest 3 over <code>adb forward</code>, <code>get_render_info</code> returns 13 draw calls and 46476 primitives from the running app, and <code>run_script</code> reads the live client state (peer id, connection, phase).</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260612-mcp-runtime-bridge-deployed-builds.html</guid>
  <pubDate>Fri, 12 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>One transport listener per authority pending http3 multi-session fixes</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260612-http3-listener-session-findings.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › One transport listener per authority pending http3 multi-session fixes</em></p>
<p><strong>Status:</strong> accepted</p>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The four-player contention smoke (the <code>loot</code> repo, <code>adapters/godot</code>) drives one <code>WebTransportPeer.create_server</code> listener in the merged assembly (<code>0518217f44</code>) with four concurrent client sessions. Three behaviors surface: incoming datagrams from every session multiplex into the server’s packet queue correctly; replies reach only one session regardless of <code>set_target_peer</code> / <code>get_packet_peer</code>; and a session teardown during traffic aborts the process with a double free (<code>quic_picoquic_backend.cpp</code>). A second <code>create_server</code> in the same process fails with “server already listening”, so the backend holds one listener per process.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: until the module carries per-session peer routing and clean teardown, the loop runs one listener per authority process, attributes requesters in the message body rather than by transport peer id, and verifies the authority’s resolutions at the harness against the Lean golden vectors. The grant-delivery path back to each client is blocked on the module fixes, which are queued as engine work: per-session ids on the MultiplayerPeer surface, teardown without the double free, and ideally multiple listeners per process.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>The four-player smoke is verifying authoritative resolution (64 rounds matching the Lean golden vectors from four concurrent clients) while grant delivery waits on the reply-routing fix.</li>
<li>The loot and combat wire parities are unaffected, because a single session per listener works end to end.</li>
<li>Issues stay disabled on the engine fork, so this record carries the findings.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>The four-player smoke passes at the harness, and a multi-session reply test starts passing per session when the module fix lands.</p>
</section>
<section id="more-information" class="level2">
<h2 class="anchored" data-anchor-id="more-information">More Information</h2>
<p>The fix is open as <a href="https://github.com/v-sekai-multiplayer-fabric/godot/pull/56">godot#56</a>: a mutex-guarded session list with MultiplayerPeer ids replaces the single slot, ingress attributes its session, egress routes by target (broadcast at zero), the drain validates session membership, and teardown erases before delete. The pattern matches the single pending-slot bug fixed in <code>fire/webtransportd@f0fc9a4</code>. Post-fix, all four clients receive their own announcements and the server survives every teardown; the one-listener-per-process limit stands. A <code>FabricMultiplayerPeer</code> probe (ENet factories injected) also routes four clients correctly and stays an alternative transport for the loop.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260612-http3-listener-session-findings.html</guid>
  <pubDate>Fri, 12 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Quest 3 immersive deployment requirements</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260612-quest3-immersive-deployment.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Quest 3 immersive deployment requirements</em></p>
<p><strong>Status:</strong> accepted</p>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>Exporting the slice to a Quest 3 surfaced several requirements that stock Godot does not satisfy by default, each of which fails in a different, non-obvious way on the device.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: the Android export carries four fixes, each confirmed on the device.</p>
<ul>
<li>Bundle the Khronos OpenXR loader into the template. Stock Godot dlopens <code>libopenxr_loader.so</code> at runtime and ships none, so OpenXR fails to start; a fetch script in <a href="https://github.com/v-sekai-multiplayer-fabric/merge">merge</a> places the arm64 loader where gradle packages it.</li>
<li>Declare <code>com.oculus.intent.category.VR</code> and <code>android.hardware.vr.headtracking</code> on the launcher activity, on <code>feat/horizonos-immersive</code>. Without the VR category Horizon hosts the app as a flat 2D panel and the OpenXR session never reaches visible.</li>
<li>Enable the <code>INTERNET</code> permission in the export. Without it the runtime MCP and any listen socket fail to open (<code>socket() == -1</code>).</li>
<li>Fix the forward-mobile vertex shader under <code>precision=double</code>, on <code>feat/double-precision-mobile</code>. The call sites pass the <code>vec4</code> <code>inv_view_precision</code> UBO field where <code>vertex_shader()</code> takes <code>vec3</code>, so every scene shader variant fails to compile and the app dies on launch; the <code>.xyz</code> fix matches the clustered renderer.</li>
</ul>
<p>A screen-state detector reads the VrApi frame heartbeat plus the display power state, so automation tells a black mirror (the Quest sleeps when the proximity sensor is uncovered) from a genuine render fault.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>The Quest export is reproducible from the merged assembly, with the engine fixes on feat branches and the loader step scripted.</li>
<li>The double-precision client runs on the Quest without origin shifting, consistent with the large-world coordinate approach.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>The APK installs, takes immersive focus (<code>VrFocusManager</code> reports it), and the on-device MCP reports a locked 72 Hz with the hub scene rendering (<a href="../decisions/20260611-quest-3-frame-floor-as-mvp-gate.html">the Quest 3 gate</a>).</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
<li><p><a href="20260611-quest-3-frame-floor-as-mvp-gate.html">Quest 3 frame floor as the MVP performance gate</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260612-quest3-immersive-deployment.html</guid>
  <pubDate>Fri, 12 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Verification smokes as a systemd podman quadlet queue</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260612-systemd-quadlet-verification-queue.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Verification smokes as a systemd podman quadlet queue</em></p>
<p><strong>Status:</strong> accepted</p>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The slice’s smokes — headless OpenXR, loot wire parity, combat wire parity, four-player contention — need to run as a repeatable queue on the workstation and on self-hosted runners, with ordering, logs, and per-job status, and the deployment decision already standardizes on podman quadlets.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: run each smoke as a oneshot quadlet <code>.container</code> unit, serialized with <code>After=</code> into a <code>fabric-verify.target</code>, because systemd supplies the queue, the journal, and the status surface with no extra orchestrator. A small <code>fabric-smoke</code> image (Fedora plus fontconfig) runs the headless smokes; the merged double-precision Godot binary, the smoke scripts, and the Lean golden vectors bind-mount read-only, so the image stays generic and the artifacts stay host-owned. Every smoke asserts against Lean-emitted golden vectors, pinned by Plausible properties in the cores.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li><code>systemctl --user start fabric-verify.target</code> runs the whole queue; <code>systemctl --user status 'fabric-smoke-*'</code> and the journal carry the results.</li>
<li>The units install by file copy into <code>~/.config/containers/systemd</code>, matching the quadlet deployment convention.</li>
<li>A failing smoke fails its unit, so the queue surfaces regressions per stage rather than as one opaque script.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>The target runs all four smokes to success under systemd on the workstation. The image, the runners, and the units live in <a href="https://github.com/v-sekai-multiplayer-fabric/fabric-verify">fabric-verify</a>.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260612-systemd-quadlet-verification-queue.html</guid>
  <pubDate>Fri, 12 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>r128 Q64.64 fixed-point as a Lean library for the cores</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260612-r128-fixed-point-as-lean-library.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › r128 Q64.64 fixed-point as a Lean library for the cores</em></p>
<p><strong>Status:</strong> accepted</p>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The deterministic cores (<a href="../decisions/20260611-deterministic-cores-integer-seeded-rng.html">the determinism decision</a>) need Q64.64 fixed-point math inside the Lean kernels, which lower to SPIR-V through <a href="../decisions/20260611-core-codegen-lean-slang.html">lean-slang</a>. The engine already vendors the C <code>r128</code> library (<code>thirdparty/misc/r128</code>), but the kernels are authored in Lean.</p>
</section>
<section id="considered-options" class="level2">
<h2 class="anchored" data-anchor-id="considered-options">Considered Options</h2>
<ul>
<li>Call the vendored C <code>r128</code> from the host only and keep the kernels in floating point.</li>
<li>Reimplement Q64.64 ad hoc inside each kernel.</li>
<li>Port <code>r128</code> to a Lean library the kernels import.</li>
</ul>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: provide <code>r128</code> as a Lean library that the cores import for their Q64.64 fixed-point math, because one Lean implementation feeds every kernel and lowers to SPIR-V over 64-bit integer pairs, while the vendored C <code>thirdparty/misc/r128</code> stays the host reference the Lean library matches. Keeping the kernels in floating point breaks determinism, and an ad-hoc Q64.64 per kernel drifts.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>The cores share one fixed-point implementation, so the host reference and the SPIR-V kernels agree bit-for-bit.</li>
<li>The Lean library lowers through lean-slang, so the fixed-point ops reach SPIR-V over 64-bit integer pairs.</li>
<li>A Plausible suite checks the Lean <code>r128</code> against the vendored C <code>r128</code> for matching results.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>The Lean <code>r128</code> library matches the vendored <code>thirdparty/misc/r128</code> on a Plausible cross-check, and the lowered SPIR-V reproduces the same results.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
<li><p><a href="20260611-core-codegen-lean-slang.html">Core kernel codegen via lean-slang (Lean to Slang to SPIR-V)</a></p></li>
<li><p><a href="20260611-deterministic-cores-integer-seeded-rng.html">Deterministic cores via r128 fixed-point and seeded RNG</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260612-r128-fixed-point-as-lean-library.html</guid>
  <pubDate>Fri, 12 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Budgeter hexagon — core, ports, and adapters</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-hexagon-budgeter-core.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Budgeter hexagon — core, ports, and adapters</em></p>
<p><strong>Tier:</strong> proof of concept · <strong>Status:</strong> accepted</p>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The slice needs graceful degradation under load on the mobile floor, dialing quality knobs per frame so the player stays inside the experience during a burst.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: structure the budgeter as a hexagon. The core is a constraint solver mapping on-device measurements to knob settings — avatar level-of-detail, interpolation against extrapolation, voice radius, and audio sample quality — as a pure reducer; it dials the knobs every frame, never overrules itself, and never dictates how the artists work.</p>
<p>Driving port: <code>measurement_source</code> (frame time, thermals, and entity counts). Driven port: <code>knob_sink</code> (the settings the runtime applies). Adapters: <code>feat/module-open-telemetry</code> feeds <code>measurement_source</code>; the engine runtime applies <code>knob_sink</code>; a fixture adapter replays a load spike for CI.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>As a hundred extra players land in the hub, the budgeter degrades gracefully, so the player stays inside the experience.</li>
<li>The solve runs against a recorded spike in CI, so the knob response is testable with no device.</li>
<li>The budgeter stays advisory, so it never overrules the artists.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>The core replays a recorded load spike and asserts the knob response, with no device under it.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-hexagon-budgeter-core.html</guid>
  <pubDate>Thu, 11 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>CASSIE desktop curvenet authoring for content, no Blender</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-cassie-desktop-curvenet-authoring.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › CASSIE desktop curvenet authoring for content, no Blender</em></p>
<p><strong>Status:</strong> accepted</p>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The content pipeline needs budget-bounded geometry for the slice without standing up Blender. The <code>feat/module-cassie</code> curve-and-surface sketcher produces curve networks that triangulate to a controllable poly count.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: author the Hub, the Field, and the enemy as CASSIE curvenets through the desktop path for the deadline, with no Blender, because a curvenet is a bounded, inspectable graph the <code>zone-baker</code> can cost, and the desktop path skips the in-headset ergonomics for the one-week build.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>The geometry arrives near budget, so it passes the baker with little rework.</li>
<li>The in-headset sketching ergonomics land after the gate.</li>
<li>The pipeline carries no Blender dependency.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>The authored Hub and Field pass the <code>zone-baker</code> at budget.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
<li><p><a href="20260611-content-build-merged-double-precision-mcp.html">Content creation in a single merged double-precision build via the editor MCP</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-cassie-desktop-curvenet-authoring.html</guid>
  <pubDate>Thu, 11 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>CI/CD runners operate as a finite queueing system</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-cicd-runners-as-queueing-system.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › CI/CD runners operate as a finite queueing system</em></p>
<p><strong>Tier:</strong> baseline · <strong>Status:</strong> accepted</p>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The organization shares one GitHub Actions runner pool across every repository, observed at roughly 14–20 concurrent jobs. A single push to the Godot fork triggers a matrix of about 20 jobs, and the slowest of them (sanitizer and Windows builds) hold a runner for one to two hours. Pushes arrive from fix branches, pull requests, merge commits, reruns, and even archive repositories that carry the same workflow files. When arrivals outpace the pool, every queued job waits behind work that may carry no information at all. How does CI work get admitted to the runner pool so that signal arrives quickly?</p>
</section>
<section id="decision-drivers" class="level2">
<h2 class="anchored" data-anchor-id="decision-drivers">Decision Drivers</h2>
<ul>
<li>The pool is a queueing system with a small fixed number of servers: as utilization approaches saturation, waiting time grows without bound (M/M/c behaviour), and the delay hits short high-signal jobs as hard as long ones.</li>
<li>One logical change can spawn several full matrices: a branch push and its pull request double-trigger, the merge push triggers again, and a rerun repeats whatever already ran.</li>
<li>Some queued work is known in advance to produce nothing: a job whose fix is not on the branch yet fails with certainty, and a run on a merged or deleted ref validates nothing.</li>
<li>Diagnostics need CI hardware (a crash that only reproduces on GCC Linux runners) but not the whole matrix.</li>
</ul>
</section>
<section id="considered-options" class="level2">
<h2 class="anchored" data-anchor-id="considered-options">Considered Options</h2>
<ul>
<li>Unbounded admission: push and rerun freely, let GitHub queue everything.</li>
<li>Hard concurrency caps in workflow configuration (per-repo <code>concurrency</code> groups beyond the existing per-branch cancellation).</li>
<li>An operating discipline that bounds arrivals at the source: batch, sequence, and cancel zero-information work.</li>
</ul>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: “an operating discipline that bounds arrivals at the source”, because the waste comes from how work is submitted, not from how GitHub schedules it — and a discipline adapts per situation where a hard cap blocks urgent work behind stale work.</p>
<p>The discipline:</p>
<ul>
<li>Two or three full matrices run at a time; further work waits for a slot. A merge push waits until the in-flight run it would concurrency-cancel finishes, so partially-completed long jobs are not thrown away mid-service.</li>
<li>Arrivals are batched: fixes land as one commit per push where possible, and a late-discovered fix joins an already-open PR branch rather than opening a new one. One merge then validates the whole batch with a single matrix.</li>
<li>Zero-information runs are cancelled immediately: the push-event run that duplicates a PR’s pull-request run, runs still queued on merged or deleted refs, runs whose remaining jobs have a known outcome, and workflow runs triggered by pushes to archive repositories (Actions stays disabled on <code>godot-archived</code>).</li>
<li>Diagnostics are trimmed to one job: a throwaway branch edits the workflow down to the single job that reproduces the problem, and reuses the warm build cache by keeping the matrix entry’s <code>cache-name</code>. The branch is deleted once the answer is in hand.</li>
<li>A failed job whose fix is not yet on the branch never reruns; until the fix lands it would occupy a server for its full service time and return a result that is already known.</li>
<li>Backlog is admitted selectively: stale branches needing fresh signal enter the queue worst-first, one or two at a time, when utilization is low rather than as a bulk rerun.</li>
</ul>
<section id="consequences" class="level3">
<h3 class="anchored" data-anchor-id="consequences">Consequences</h3>
<ul>
<li>Good: high-signal jobs spend their time in service, not in queue; verdicts on key jobs arrive in roughly the service time of the job itself.</li>
<li>Good: the merge policy (merge on a passing key job) composes cleanly — batched PRs mean each merge buys validation for several fixes with one matrix.</li>
<li>Bad: sequencing adds wall-clock latency to individual merges; a merge-ready PR waits for an unrelated in-flight run to finish.</li>
<li>Bad: the discipline lives in operator habit and this record, not in enforced configuration; a contributor who pushes freely still floods the pool.</li>
</ul>
</section>
<section id="confirmation" class="level3">
<h3 class="anchored" data-anchor-id="confirmation">Confirmation</h3>
<p>The discipline is in practice on the CHI-364 CI campaign: six redundant queued runs cancelled in one sweep (two merged PRs’ leftovers, a deleted tag’s run, a push/PR duplicate), Actions disabled on <code>godot-archived</code> after an archive push was found competing with the main repo’s queue, a GCC-only segfault diagnosed through a single-job trimmed workflow over four iterations on a warm cache, and fixes batched so that two branches reached green with one validation matrix each.</p>
</section>
</section>
<section id="more-information" class="level2">
<h2 class="anchored" data-anchor-id="more-information">More Information</h2>
<p>The queue is observable with <code>gh run list --repo &lt;repo&gt; --json databaseId,status,headBranch</code> filtered to <code>queued</code>/<code>in_progress</code>, and per-run job counts with <code>gh run view &lt;id&gt; --json jobs</code>. Little’s law gives the sanity check: jobs in system ≈ arrival rate × time in system, so when queued jobs exceed the server count several times over, every new matrix admits about an hour of added latency to its own verdict.</p>


</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-cicd-runners-as-queueing-system.html</guid>
  <pubDate>Thu, 11 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Combat hexagon — core, ports, and adapters</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-hexagon-combat-core.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Combat hexagon — core, ports, and adapters</em></p>
<p><strong>Tier:</strong> proof of concept · <strong>Status:</strong> accepted</p>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The combat concern — combo timing, hit validation, the enemy spawn-invulnerability window, and damage — needs to be deterministic and testable with no server and no headset. Per <a href="../decisions/20260610-hexagonal-core-ports-adapters.html">the hexagonal decision</a> it becomes a core behind narrow ports.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: structure combat as a hexagon. The core resolves the combo timing, validates each hit against the authoritative positions, holds the enemy invulnerable for its spawn window, and deducts health, as a pure reducer (<a href="../decisions/20260611-core-contract-pure-reducer-byte-state.html">the core contract</a>) over deterministic state (<a href="../decisions/20260611-deterministic-cores-integer-seeded-rng.html">the determinism decision</a>).</p>
<p>Driving ports: <code>input_source</code> (player commands with timestamps and targets), <code>tick_source</code> (the constant-step clock), <code>behavior_source</code> (enemy intents). Driven ports: <code>state_sink</code> (authoritative entity state), <code>event_sink</code> (hits, deaths, door unlocks). Adapters: <code>feat/module-http3</code> feeds <code>input_source</code>; the <code>zone-server</code> hosts the core and drives <code>tick_source</code> under <a href="../decisions/20260611-server-authoritative-simulation-deferred-rollback.html">server authority</a>; <a href="../decisions/20260611-generated-behavior-sandboxed-riscv.html">sandboxed behavior</a> implements <code>behavior_source</code>; a fixture adapter replays recorded inputs for CI.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>The server-authoritative adapter binds <code>input_source</code> and <code>state_sink</code> with interpolation and no prediction for the deadline; the rollback adapter lands after the gate behind the same ports.</li>
<li>The combat core runs headless against fixtures, so a flaky adapter fails in isolation.</li>
<li>The melee archetype ships first; the ranged and caster archetypes land after the gate.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>The combat core passes its fixtures, and a melee combo lands a hit under server authority on a Quest 3.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
<li><p><a href="20260610-hexagonal-core-ports-adapters.html">Hexagonal core/ports/adapters as the component convention</a></p></li>
<li><p><a href="20260611-core-contract-pure-reducer-byte-state.html">Cores as pure reducers over byte-serialized state</a></p></li>
<li><p><a href="20260611-deterministic-cores-integer-seeded-rng.html">Deterministic cores via r128 fixed-point and seeded RNG</a></p></li>
<li><p><a href="20260611-generated-behavior-sandboxed-riscv.html">Generated behavior runs as sandboxed RISC-V</a></p></li>
<li><p><a href="20260611-server-authoritative-simulation-deferred-rollback.html">Server-authoritative simulation with deferred rollback</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-hexagon-combat-core.html</guid>
  <pubDate>Thu, 11 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Content creation in a single merged double-precision build via the editor MCP</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-content-build-merged-double-precision-mcp.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Content creation in a single merged double-precision build via the editor MCP</em></p>
<p><strong>Status:</strong> accepted</p>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>Content creation needs the full engine, every <code>feat/*</code> capability at double precision, without Blender, and the team budgets a single such build for the slice.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: produce one Godot build by merging all <code>feat/*</code> branches with the <code>merge</code> tool at <code>precision=double</code> (<a href="../decisions/20260501-godot-double-precision-template-release-for-zone.html">the double-precision decision</a>, <a href="../decisions/20260606-compiling-godot-engine.html">the compiling decision</a>), and drive content creation through the <code>vsekai-godot-mcp</code> editor server, where the Godot editor is the MCP server over streamable-HTTP, because one merged build covers authoring and the MCP lets a content tool drive the editor directly. The pipeline uses no Blender (<a href="../decisions/20260611-cassie-desktop-curvenet-authoring.html">the CASSIE authoring decision</a>).</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>One merged double-precision build covers content authoring for the slice.</li>
<li>A content tool drives the editor through the MCP over streamable-HTTP.</li>
<li>The pipeline carries no Blender dependency.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>The merged double-precision build opens and the MCP drives an authoring round trip.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
<li><p><a href="20260501-godot-double-precision-template-release-for-zone.html">Godot double precision template_release for zone servers</a></p></li>
<li><p><a href="20260606-compiling-godot-engine.html">Compiling the Godot engine</a></p></li>
<li><p><a href="20260611-cassie-desktop-curvenet-authoring.html">CASSIE desktop curvenet authoring for content, no Blender</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-content-build-merged-double-precision-mcp.html</guid>
  <pubDate>Thu, 11 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Core kernel codegen via lean-slang (Lean to Slang to SPIR-V)</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-core-codegen-lean-slang.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Core kernel codegen via lean-slang (Lean to Slang to SPIR-V)</em></p>
<p><strong>Status:</strong> accepted</p>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The cores carry compute kernels — hit raycasts, the budgeter solve, geometry costing — that need to be verified once and run as GPU compute, with the dispatch wrapped behind the flat C ABI port (<a href="../decisions/20260611-lean4-kernel-cores-flat-c-host-adapters.html">the Lean cores decision</a>). Hand-porting a kernel from a separate spec to a shader drifts.</p>
</section>
<section id="decision-drivers" class="level2">
<h2 class="anchored" data-anchor-id="decision-drivers">Decision Drivers</h2>
<ul>
<li>One verified Lean source per kernel, with no separate shader to drift.</li>
<li>The kernel runs on the GPU and integrates with GPU convergence; the CPU Slang target lacks that integration.</li>
<li>Integer ops keep the kernel deterministic across conformant devices.</li>
</ul>
</section>
<section id="considered-options" class="level2">
<h2 class="anchored" data-anchor-id="considered-options">Considered Options</h2>
<ul>
<li>Hand-write each kernel as a shader from a separate spec.</li>
<li>Lower a CPU host target through Slang.</li>
<li>Author the kernel in Lean and lower it to SPIR-V.</li>
</ul>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: author the compute kernels in Lean and lower them to Slang through <a href="https://github.com/V-Sekai-fire/lean-slang">lean-slang</a>, then run <code>slangc -target spirv</code> to SPIR-V (<code>.spv</code>), and dispatch the <code>.spv</code> behind the flat C ABI, because SPIR-V runs the kernel on the GPU with the convergence integration the CPU Slang target lacks, and one verified Lean source produces the shipped kernel. The <code>idtx-flow</code> repository already depends on <code>LeanSlang</code> and byte-pins the emitted Slang against committed source with <code>native_decide</code>.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>One Lean kernel source produces the <code>.spv</code>, so the spec and the shipped kernel stay one artifact.</li>
<li>The lowered Slang and the <code>.spv</code> are committed and byte-pinned, so a regen that drifts fails the pin.</li>
<li>The kernels use integer ops, so the SPIR-V stays deterministic across conformant devices (<a href="../decisions/20260611-deterministic-cores-integer-seeded-rng.html">the determinism decision</a>).</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>The emitted Slang matches its committed byte-pin, and the dispatched <code>.spv</code> kernel passes the core fixtures.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
<li><p><a href="20260611-core-contract-pure-reducer-byte-state.html">Cores as pure reducers over byte-serialized state</a></p></li>
<li><p><a href="20260611-deterministic-cores-integer-seeded-rng.html">Deterministic cores via r128 fixed-point and seeded RNG</a></p></li>
<li><p><a href="20260611-lean4-kernel-cores-flat-c-host-adapters.html">Lean 4 build-time kernel cores with flat-C host adapters</a></p></li>
<li><p><a href="20260612-r128-fixed-point-as-lean-library.html">r128 Q64.64 fixed-point as a Lean library for the cores</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-core-codegen-lean-slang.html</guid>
  <pubDate>Thu, 11 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Cores as pure reducers over byte-serialized state</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-core-contract-pure-reducer-byte-state.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Cores as pure reducers over byte-serialized state</em></p>
<p><strong>Status:</strong> accepted</p>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>Every hexagonal core (<a href="../decisions/20260610-hexagonal-core-ports-adapters.html">the hexagonal decision</a>) needs to be replayable, snapshot-able, fixture-testable, and transport-agnostic. A core that hides mutable state behind methods defeats all four.</p>
</section>
<section id="considered-options" class="level2">
<h2 class="anchored" data-anchor-id="considered-options">Considered Options</h2>
<ul>
<li>Stateful objects whose internal state lives behind methods.</li>
<li>A pure reducer over an explicit, serializable state value.</li>
</ul>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: model each core as a pure reducer, <code>step : State -&gt; Event -&gt; State x Effects</code>, with <code>State</code>, <code>Event</code>, and <code>Effects</code> serialized to bytes deterministically, so the core is bytes to bytes. The flat C ABI exposes <code>step</code> plus <code>snapshot</code> and <code>restore</code> over the byte state, and the compute kernels lower through <a href="../decisions/20260611-core-codegen-lean-slang.html">lean-slang</a> to SPIR-V, dispatched on the GPU. Hidden mutable state defeats snapshots and exact fixtures, so the first option loses replay and rollback.</p>
<p>With <a href="../decisions/20260611-deterministic-cores-integer-seeded-rng.html">the determinism decision</a>, this makes replay byte-exact and snapshots a value copy.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>Snapshots and rollback are copies of the state bytes.</li>
<li>Fixtures pin exact output bytes, so a divergence shows up as a failing fixture.</li>
<li>The same <code>step</code> backs every adapter, so <code>webtransportd</code> and the engine carry the same bytes.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>Replaying an event log reproduces the state bytes on every target.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
<li><p><a href="20260610-hexagonal-core-ports-adapters.html">Hexagonal core/ports/adapters as the component convention</a></p></li>
<li><p><a href="20260611-core-codegen-lean-slang.html">Core kernel codegen via lean-slang (Lean to Slang to SPIR-V)</a></p></li>
<li><p><a href="20260611-deterministic-cores-integer-seeded-rng.html">Deterministic cores via r128 fixed-point and seeded RNG</a></p></li>
<li><p><a href="20260611-hexagon-combat-core.html">Combat hexagon — core, ports, and adapters</a></p></li>
<li><p><a href="20260611-server-authoritative-simulation-deferred-rollback.html">Server-authoritative simulation with deferred rollback</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-core-contract-pure-reducer-byte-state.html</guid>
  <pubDate>Thu, 11 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Deterministic cores via r128 fixed-point and seeded RNG</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-deterministic-cores-integer-seeded-rng.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Deterministic cores via r128 fixed-point and seeded RNG</em></p>
<p><strong>Status:</strong> accepted</p>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>Replay, recorded fixtures, and the future rollback adapter need the authoritative cores to be bit-exact across the RTX 4090 workstation, the Quest 3, and the Steam Deck. IEEE-754 floating point diverges across platforms through fused multiply-add, instruction reordering, and transcendental functions, so a float in the authoritative path breaks replay silently.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: the authoritative cores use r128 Q64.64 fixed-point and a seeded RNG threaded through state, with no floating point, because fixed-point over 64-bit integers reproduces bit-exact across machines while an explicit seed in the state makes the loot roll replayable. The cores import r128 as a Lean library (<a href="../decisions/20260612-r128-fixed-point-as-lean-library.html">the r128 Lean library decision</a>) that lowers to SPIR-V, and the engine’s vendored <code>thirdparty/misc/r128</code> stays the host reference.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>Replay and snapshots stay byte-exact, so the rollback adapter becomes cheap.</li>
<li>The fixtures pin exact outputs, so a divergence shows up as a failing fixture rather than a silent drift.</li>
<li>The loot roll is reproducible from the seed in the state.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>Replaying an input log reproduces the state hash on every target.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
<li><p><a href="20260611-core-codegen-lean-slang.html">Core kernel codegen via lean-slang (Lean to Slang to SPIR-V)</a></p></li>
<li><p><a href="20260611-core-contract-pure-reducer-byte-state.html">Cores as pure reducers over byte-serialized state</a></p></li>
<li><p><a href="20260611-hexagon-combat-core.html">Combat hexagon — core, ports, and adapters</a></p></li>
<li><p><a href="20260612-r128-fixed-point-as-lean-library.html">r128 Q64.64 fixed-point as a Lean library for the cores</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-deterministic-cores-integer-seeded-rng.html</guid>
  <pubDate>Thu, 11 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>First-party curated content with zone-baker budgets</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-first-party-curated-content-zone-baker-budgets.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › First-party curated content with zone-baker budgets</em></p>
<p><strong>Tier:</strong> baseline · <strong>Status:</strong> accepted</p>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>A user-generated-content runtime carries arbitrary, unpredictable per-frame cost, which is fatal on a mobile GPU holding a stereo VR frame. The MVP needs predictable cost and the freedom to co-optimize content against the engine.</p>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: ship first-party curated content only, with no user-generated-content runtime, and let the <code>zone-baker</code> enforce hard budgets at bake time, because curated content lets the engine co-optimize with the art and lets the baker reject anything over budget before it reaches a device.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>A four-player Field room holds its geometry, four avatars, and props under roughly 500,000 visible triangles and 200 draw calls per eye on the Quest 3.</li>
<li>The baker rejects any asset that exceeds the budget, so cost stays predictable.</li>
<li>The content surface stays small, which keeps the determinism and the budgeter tractable.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>Every shipped asset passes the <code>zone-baker</code> at budget, and a four-player room holds the per-eye limits on a standalone Quest 3.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-first-party-curated-content-zone-baker-budgets.html</guid>
  <pubDate>Thu, 11 Jun 2026 00:00:00 GMT</pubDate>
</item>
<item>
  <title>Forward renderer with baked light for the mobile floor</title>
  <link>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-forward-renderer-baked-light.html</link>
  <description><![CDATA[ 





<p><em><a href="../index.html">Manuals</a> › <a href="../decisions.html">Decisions</a> › Forward renderer with baked light for the mobile floor</em></p>
<p><strong>Status:</strong> accepted</p>
<section id="context-and-problem-statement" class="level2">
<h2 class="anchored" data-anchor-id="context-and-problem-statement">Context and Problem Statement</h2>
<p>The Quest 3 rations bandwidth across file, memory, and network IO, and deferred rendering competes for it. The slice needs predictable frame cost regardless of light count on the mobile tile renderer.</p>
</section>
<section id="considered-options" class="level2">
<h2 class="anchored" data-anchor-id="considered-options">Considered Options</h2>
<ul>
<li>Deferred rendering.</li>
<li>A simple forward renderer with baked global illumination.</li>
</ul>
</section>
<section id="decision-outcome" class="level2">
<h2 class="anchored" data-anchor-id="decision-outcome">Decision Outcome</h2>
<p>Chosen option: a simple forward renderer with baked global illumination and light probes for static geometry, a dedicated shadow pass for avatars, and probe lighting for dynamic entities, because the frame cost stays predictable regardless of light count. Deferred rendering pressures the bandwidth the mobile tile renderer rations.</p>
</section>
<section id="consequences" class="level2">
<h2 class="anchored" data-anchor-id="consequences">Consequences</h2>
<ul>
<li>Frame cost stays predictable, so artists place many lights without watching the budget.</li>
<li>Dynamic entities take lower-fidelity probe lighting.</li>
<li>The renderer removes one axis the small team otherwise tunes by hand.</li>
</ul>
</section>
<section id="confirmation" class="level2">
<h2 class="anchored" data-anchor-id="confirmation">Confirmation</h2>
<p>The Field room holds 72 Hz on a standalone Quest 3 with many lights placed.</p>


</section>
<section id="related" class="level2">
<h2 class="anchored" data-anchor-id="related">Related</h2>
<ul>
<li><p><a href="../changelog/2026/20260612-deck-log.html">V-Sekai Multiplayer Fabric Deck Log — 2026-06-12 (Friday)</a></p></li>
</ul>
</section>

 ]]></description>
  <guid>https://v-sekai-multiplayer-fabric.github.io/manuals/decisions/20260611-forward-renderer-baked-light.html</guid>
  <pubDate>Thu, 11 Jun 2026 00:00:00 GMT</pubDate>
</item>
</channel>
</rss>
